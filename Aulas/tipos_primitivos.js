const nome = 'Fernando'; //String
const numero = 1; //number
let nomeAluno; //undefined, não aponta para local nenhum na memória.
const sobreNomeAluno = null; //nulo, tambem não aponta para local nenhum na memória.
const aprovado = false; //boolean, valor lógico (true or false)